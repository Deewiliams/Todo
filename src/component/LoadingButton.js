import { CircularProgress } from "@mui/material";
import React from "react";

const LoadingButton = () => {
  return <CircularProgress size={24} style={{ color: "white" }} />;
};

export default LoadingButton;
